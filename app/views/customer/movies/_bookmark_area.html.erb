<% if movie.bookmark_by?(current_customer) %>
    <%= link_to 'ブックマーク解除', customer_movie_bookmarks_path(movie.id), method: :delete, remote: true, id: :"js-bookmark-button-for-movie-#{movie.id}" %>
<% else %>
        <%= link_to 'ブックマーク', customer_movie_bookmarks_path(movie.id), method: :post, remote: true, id: :"js-bookmark-button-for-movie-#{movie.id}"%>
<% end %>