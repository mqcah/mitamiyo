<%= form_with(model: current_customer.following_relationships.find_by(following_id: @customer.id),method: :delete) do |f| %>
  <%= f.hidden_field :following_id %>  
  <%= f.submit "フォロー", class: "btn btn-outline-secondary" %>
<% end %>