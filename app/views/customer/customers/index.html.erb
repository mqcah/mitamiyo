<% @customers.each do |customer| %>

<!-- アイコン -->
<%= attachment_image_tag customer, :image, :fill, 280, 210, format: 'jpeg' %>

<!-- ユーザーネーム -->
<%= link_to customer.customer_name, customer_path(customer.id) %>

<!-- ユーザーID -->
<%= '@' + customer.user_id %>
<% end %>